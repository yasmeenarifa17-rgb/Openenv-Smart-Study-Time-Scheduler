name: Smart Study Time Scheduler
description: RL environment for optimizing study schedules
version: 1.0

entrypoint:
  type: http
  url: http://localhost:7860

endpoints:
  reset: /reset
  step: /step
  state: /state
